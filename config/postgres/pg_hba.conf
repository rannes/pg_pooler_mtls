# Administrative maintenance remains local and OS-user authenticated.
local all postgres peer
local all all reject
# Network reachability is limited separately by the backend Docker bridge.
hostssl productdb app1,app2 all cert map=gateways
host all all all reject
