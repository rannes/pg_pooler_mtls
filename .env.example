# Change ports if already occupied. Only loopback HTTP ports are published.
APP1_PORT=8081
APP2_PORT=8082
# Change BOTH together if this subnet overlaps an existing Docker/VPN network.
BACKEND_SUBNET=172.30.83.0/24
DIRECT_DB_IP=172.30.83.10
